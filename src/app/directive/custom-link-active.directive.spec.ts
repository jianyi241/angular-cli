import {CustomLinkActiveDirective} from './custom-link-active.directive';

describe('CustomLinkActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomLinkActiveDirective();
    expect(directive).toBeTruthy();
  });
});
