import {ReviewDropdownDirective} from './review-dropdown.directive';

describe('ReviewDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ReviewDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
