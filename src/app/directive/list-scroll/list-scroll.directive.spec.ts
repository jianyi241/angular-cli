import { ListScrollDirective } from './list-scroll.directive';

describe('ListScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ListScrollDirective();
    expect(directive).toBeTruthy();
  });
});
