import { TogglePwdDirective } from './toggle-pwd.directive';

describe('TogglePwdDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglePwdDirective();
    expect(directive).toBeTruthy();
  });
});
