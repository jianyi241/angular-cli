import { PopoverCustomDirective } from './popover-custom.directive';

describe('PopoverCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverCustomDirective();
    expect(directive).toBeTruthy();
  });
});
